import { GiftCard } from "../../components/gift-card";

export const metadata = {
  title: "Dashboard",
};

const Dashboard = () => {
  return (
    <div>
      <span>.dashboard</span>
      <GiftCard />
    </div>
  );
};

export default Dashboard;
